
    CONTAINER_NAME:
        build: .
        container_name: CONTAINER_NAME
        volumes:
            - .:/crypto-trader-bot
        environment:
            - TZ=Europe/Amsterdam
            - CONF=CONTAINER_NAME
        depends_on:
            - influxdb
            - grafana
        restart: always
